(define (Hanoi n)
  (- (expt 2 n) 1))
;(Hanoi 1)
(define (line-splite n)
  (+ (/ (* n (+ n 1)) 2) 1))

;(line-splite 0)
;(line-splite 2)


(define (square n)
  (* n n))
(define (variant-splite n)
  (+ (* 2 (square n)) (- n) 1))

(variant-splite 2)

